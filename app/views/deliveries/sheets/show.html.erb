<header class="mb-8 text-center print:hidden">
  <h1 class="font-bold text-2xl/tight">Delivery Note for Order #<%= @order.id %></h1>
</header>

<div class="mx-auto max-w-screen-md">
  <%= render partial: 'sheet', locals: { order: @order } %>
</div>