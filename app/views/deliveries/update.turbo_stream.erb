<%# app/views/deliveries/update.turbo_stream.erb %>

<%= turbo_stream.replace 'pending_section' do %>
  <%= turbo_frame_tag 'pending_section', src: deliveries_path %>
<% end %>

<%= turbo_stream.replace 'schedule' do %>
  <%= turbo_frame_tag 'schedule', src: deliveries_path(date: @order.delivery_date.to_date) %>
<% end %>
