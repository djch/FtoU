<header class="mb-6">
  <h1 class="font-bold text-2xl/tight">Editing Customer: <%= @customer.name %></h1>
</header>

<%= render 'form' %>
<%= link_to 'Never mind, go back', customer_path(@customer) %>